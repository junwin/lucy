[pytest]
minversion = 7.0
addopts = -q -ra --strict-markers

testpaths =
    tests

pythonpath =
    src

# Keep marker list explicit to avoid accidental typos.
markers =
    unit: unit tests
    integration: integration tests
